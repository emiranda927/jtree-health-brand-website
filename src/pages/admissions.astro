---
import Base from '../layouts/Base.astro';
import PageHero from '../components/PageHero.astro';

const turnstileKey = import.meta.env.PUBLIC_TURNSTILE_SITE_KEY || '';

const steps = [
  { n: '01', t: 'Fill out the form', b: 'A two-minute form. No medical details — we’ll cover those by phone.' },
  { n: '02', t: 'We call you', b: 'A conversation about what your teen needs, within one business day.' },
  { n: '03', t: 'Start within 3–5 days', b: 'We recommend the right level of care and build a plan together. No pressure.' },
];
const ages = Array.from({ length: 8 }, (_, i) => 10 + i);
const programs = ['IOP', 'PHP', 'Unsure'];
const times = ['Morning', 'Afternoon', 'Evening'];
const sources = ['Google', 'Referral', 'Doctor', 'School', 'Other'];
const insurances = ['Aetna', 'BCBS NC', 'Cigna', 'UnitedHealthcare', 'UMR', 'Self-pay', 'Other'];
---
<Base
  title="Admissions — Start the Conversation | Joshua Tree Health"
  description="Request a callback from Joshua Tree Health. No waitlist, response within one business day, in-network with major insurers. Adolescent PHP & IOP in Apex, NC."
>
  <PageHero
    title="Let's find the right fit for <em>your family</em>."
    sub="Fill out the short form below and we'll call you within one business day. No commitment, just a conversation about what your teen needs."
  />

  <section class="section section--tight" aria-label="How it works">
    <div class="container-x">
      <ol class="process__steps" data-reveal-group>
        {steps.map((s) => (
          <li class="step" data-reveal-item>
            <span class="step__num">{s.n}</span>
            <h2 class="step__title">{s.t}</h2>
            <p class="step__body">{s.b}</p>
          </li>
        ))}
      </ol>
    </div>
  </section>

  <section class="section surface-cream-deep" aria-labelledby="form-title">
    <div class="container-x admissions">
      <div class="admissions__intro" data-reveal>
        <p class="eyebrow">Request a callback</p>
        <h2 class="section-title font-display" id="form-title">Two minutes. Then we call you.</h2>
        <p class="section-sub">No medical details here — we'll cover everything by phone. If we're not the right fit, we'll tell you honestly and help you find what is.</p>
        <ul class="checklist admissions__assure">
          <li>Response within one business day</li>
          <li>We verify your insurance before you start</li>
          <li>No waitlist, no obligation</li>
        </ul>
      </div>

      <div class="admissions__formwrap card" data-reveal data-reveal-delay="0.08">
        <form class="form" id="inquiry-form" novalidate>
          <div class="form-error-banner" role="alert" aria-live="assertive"></div>

          <div class="form__row form__row--2">
            <div class="field field--req">
              <label for="name">Your name</label>
              <input id="name" name="name" type="text" autocomplete="name" required />
              <span class="jth-field-error" id="err-name"></span>
            </div>
            <div class="field field--req">
              <label for="email">Email</label>
              <input id="email" name="email" type="email" autocomplete="email" required />
              <span class="jth-field-error" id="err-email"></span>
            </div>
          </div>

          <div class="form__row form__row--2">
            <div class="field field--req">
              <label for="phone">Phone</label>
              <input id="phone" name="phone" type="tel" autocomplete="tel" required />
              <span class="jth-field-error" id="err-phone"></span>
            </div>
            <div class="field field--req">
              <label for="teen_age">Teen's age</label>
              <select id="teen_age" name="teen_age" required>
                <option value="">Select…</option>
                {ages.map((a) => <option value={a}>{a}</option>)}
              </select>
              <span class="jth-field-error" id="err-teen_age"></span>
            </div>
          </div>

          <div class="form__row form__row--2">
            <div class="field field--req">
              <label for="program_interest">Program of interest</label>
              <select id="program_interest" name="program_interest" required>
                <option value="">Select…</option>
                {programs.map((p) => <option value={p}>{p}</option>)}
              </select>
              <span class="jth-field-error" id="err-program_interest"></span>
            </div>
            <div class="field">
              <label for="best_time_to_call">Best time to call <span class="field__opt">(optional)</span></label>
              <select id="best_time_to_call" name="best_time_to_call">
                <option value="">No preference</option>
                {times.map((t) => <option value={t}>{t}</option>)}
              </select>
            </div>
          </div>

          <div class="form__row form__row--2">
            <div class="field">
              <label for="insurance">Insurance <span class="field__opt">(optional)</span></label>
              <select id="insurance" name="insurance">
                <option value="">If you know it…</option>
                {insurances.map((i) => <option value={i}>{i}</option>)}
              </select>
            </div>
            <div class="field">
              <label for="zip">ZIP code <span class="field__opt">(optional)</span></label>
              <input id="zip" name="zip" type="text" inputmode="numeric" autocomplete="postal-code" maxlength="5" />
            </div>
          </div>

          <div class="form__row form__row--2">
            <div class="field">
              <label for="how_did_you_hear">How did you hear about us? <span class="field__opt">(optional)</span></label>
              <select id="how_did_you_hear" name="how_did_you_hear">
                <option value="">Select…</option>
                {sources.map((s) => <option value={s}>{s}</option>)}
              </select>
            </div>
            <div class="field"></div>
          </div>

          <div class="form__row">
            <div class="field">
              <label for="notes">Anything you'd like us to know? <span class="field__opt">(optional)</span></label>
              <textarea id="notes" name="notes" rows="3" maxlength="1000" placeholder="A sentence about what's going on is helpful — but totally optional. No medical details needed."></textarea>
            </div>
          </div>

          <p class="form__disclaimer">By submitting, you're asking JTree Health to contact you about care. We'll only use your information to respond, and we'll never share it.</p>

          {/* Honeypot — hidden from humans, catches bots */}
          <div class="hp" aria-hidden="true">
            <label for="hp_field">Leave this field empty</label>
            <input id="hp_field" name="hp_field" type="text" tabindex="-1" autocomplete="off" />
          </div>
          <input type="hidden" id="session_id" name="session_id" value="" />

          {turnstileKey && <div id="cf-turnstile" class="cf-turnstile-box"></div>}

          <button type="submit" class="btn form__submit">Start the Conversation <span class="btn__arrow">→</span></button>
          <p class="form__note">Prefer to talk now? Call <a href="tel:+19193355053" class="form__note-link">(919) 335-5053</a>.</p>
        </form>
      </div>
    </div>
  </section>
</Base>

{turnstileKey && (
  <script src="https://challenges.cloudflare.com/turnstile/v0/api.js" async defer is:inline></script>
)}
<script>
  import '../lib/inquiry';
</script>

<style>
  .admissions { display: grid; grid-template-columns: 1fr; gap: clamp(2rem, 1rem + 3vw, 3.5rem); align-items: start; }
  .admissions__assure { margin-top: 1.6rem; }
  .admissions__formwrap { padding: clamp(1.5rem, 1rem + 2vw, 2.5rem); }
  .section--tight { padding-block: clamp(2.5rem, 2rem + 3vw, 4rem); }

  .form-error-banner { display: none; padding: 0.8rem 1rem; border-radius: var(--radius); background: #f7e2dd; border: 1px solid #e0a99c; color: #8a2f1f; font-size: 0.9rem; }
  .form-error-banner.is-visible { display: block; }
  .jth-field-error { font-size: 0.78rem; color: #a8341f; min-height: 0; }
  .jth-field-error:empty { display: none; }
  .field [aria-invalid="true"] { border-color: #c0492f; box-shadow: 0 0 0 3px rgba(168, 52, 31, 0.12); }
  .form__note-link { color: var(--color-pine); border-bottom: 1.5px solid var(--color-lime); }
  .field__opt { font-weight: 400; color: var(--color-ink-faint); font-size: 0.82em; }
  .form__disclaimer { font-size: 0.8rem; color: var(--color-ink-faint); line-height: 1.5; margin: 0; }
  .hp { position: absolute; left: -9999px; width: 1px; height: 1px; overflow: hidden; }
  .cf-turnstile-box { min-height: 65px; }
  .btn.is-loading { opacity: 0.85; cursor: progress; }

  @media (min-width: 880px) { .admissions { grid-template-columns: 1fr 1.05fr; } }
</style>
